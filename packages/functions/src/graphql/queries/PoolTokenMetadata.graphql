query poolTokenMetadata($id: ID!) {
  poolToken(id: $id) {
    id
    principalAmount
    loan {
      id
      termEndTime
      usdcApy
      fundingLimit
      principalAmount
      principalAmountRepaid
      interestAmountRepaid
      repaymentFrequency
      termInSeconds
    }
  }
}
