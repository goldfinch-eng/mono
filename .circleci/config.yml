# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1

# Define a job to be invoked later in a workflow.
# See: https://circleci.com/docs/2.0/configuration-reference/#jobs
jobs:
  test:
    # Specify the execution environment. You can specify an image from Dockerhub or use one of our Convenience Images from CircleCI's Developer Hub.
    # See: https://circleci.com/docs/2.0/configuration-reference/#docker-machine-macos-windows-executor
    docker:
      - image: cimg/node:12.18.3
    resource_class: xlarge
    # Add steps to the job
    # See: https://circleci.com/docs/2.0/configuration-reference/#steps
    steps:
      - checkout
      - restore_cache:
          # See the configuration reference documentation for more details on using restore_cache and save_cache steps
          # https://circleci.com/docs/2.0/configuration-reference/?section=reference#save_cache
          keys:
            - node-deps-v1-{{ .Branch }}-{{checksum "package-lock.json"}}
      - run:
          name: Update Ubuntu
          command: |
            sudo apt-add-repository universe
            sudo apt-get update
      - run:
          name: Install Python
          command: sudo apt-get install python
      - run:
          name: Build
          command: |
            npx lerna bootstrap --ci
            npx lerna run build --scope @goldfinch-eng/protocol --scope @goldfinch-eng/utils
      - save_cache:
          key: node-deps-v1-{{ .Branch }}-{{checksum "package-lock.json"}}
          paths:
            - ~/.npm
            - ./**/node_modules/
            - ./packages/protocol/artifacts
            - ./packages/protocol/cache
            - ./packages/protocol/typechain
            - ./packages/utils/dist
      - run:
          name: Test
          command: npx lerna run test --scope @goldfinch-eng/protocol --stream

# Invoke jobs via workflows
# See: https://circleci.com/docs/2.0/configuration-reference/#workflows
workflows:
  protocol:
    jobs:
      - test